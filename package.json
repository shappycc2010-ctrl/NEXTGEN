{
  "name": "nextgen-server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "dependencies": {
    "express": "^4.18.0",
    "node-fetch": "^3.3.1",
    "dotenv": "^16.0.0",
    "body-parser": "^1.19.0"
  }
}
{
  "name": "nextgen",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix server"
  }
}
