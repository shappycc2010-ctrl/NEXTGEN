{
  "name": "nextgen",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix server",
    "dev": "nodemon server/index.js"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "express": "^4.18.0",
    "node-fetch": "^3.3.1",
    "dotenv": "^16.0.0",
    "body-parser": "^1.19.0"
  }
}
